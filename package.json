{
  "name": "stars-giveaway-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot index.ts",
    "start": "bun index.ts",
    "postinstall": "prisma generate",
    "setup:postgres": "prisma generate && prisma db push --accept-data-loss"
  },
  "dependencies": {
    "@prisma/client": "^6.11.1",
    "prisma": "^6.11.1"
  },
  "devDependencies": {
    "bun-types": "^1.3.4"
  }
}
